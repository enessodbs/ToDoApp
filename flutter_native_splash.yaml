flutter_native_splash:
  color: "#FFFFFF"  # Arka plan rengi
  image: assets/splash.png  # Splash ekranında kullanılacak görüntü

  android_12:
    # Android 12 ve sonrası için aynı splash görüntüsünü kullanıyoruz.
    image: assets/splash.png

  # Diğer platformlar için de aynı splash görüntüsünü kullanıyoruz.
  # Eğer sadece belirli platformlarda bu resmi kullanmak istiyorsanız, aşağıdaki alanları aktif hale getirin.
  # image_android: assets/splasshImage.png
  # image_dark_android: assets/splasshImage.png
  # image_ios: assets/splasshImage.png
  # image_dark_ios: assets/splasshImage.png
  # image_web: assets/splasshImage.png
  # image_dark_web: assets/splasshImage.png
